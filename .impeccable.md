# Design Context — JobPilot

> 本文件供 `impeccable` skill 读取。设计上下文**不可从代码推断**，以下内容摘自项目权威文档
> `JobPilot_UIUX设计规范.md`（v1.0）与 `JobPilot_PRD.md`，不是新做的设计决策。
> 任何与 `globals.css` 实际实现冲突之处，以本文档 + 规范原文为准。

---

## 1. Target audience（谁在使用）

**求职中的技术/知识工作者**，主要在**桌面浏览器**上于晚间集中操作。

- 典型情境：拿到一份 JD → 想快速知道「我差在哪」→ 逐条修改简历 → 导出投递。
- 使用心态：**对 AI 输出高度不信任**。他们见过太多"AI 写得漂亮但全是编的"简历，
  且编造内容在面试中会直接崩盘。因此**可信度 > 视觉炫技**。
- 情绪状态：求职本身带焦虑，界面**不应制造额外的"还要多久""这是不是又卡了"的焦虑**。

## 2. Use cases（要完成什么任务）

主链路（V1）：`登录 → 首页 → 我的简历 → 解析确认 → 简历分析 → 分析岗位(JD) → 匹配 → 修改建议 → PDF 导出`

核心任务：**把 AI 抽取出的事实逐条交还给用户确认**，并让每个 AI 判断都可点开看证据。
次级任务：投递进度管理（`/applications`，极简）。

## 3. Brand personality / tone（应该给人什么感觉）

**专业、清晰、现代的生产力工具**。不是消费级玩具，不是"AI 魔法"演示。

- 感觉应该是：**冷静、可核验、克制、有秩序**。
- 明确不要的感觉：科幻感、未来感、"AI 思考中"的炫技感、活泼卖萌。

---

## 4. 七条可验收设计原则（硬约束，来自 UIUX 规范 §0）

| # | 原则 | 落地规则 |
|---|------|---------|
| 1 | 信息层级清晰 | 每页**只允许一个 h1**；正文三级（20/14/13px）；同时出现的强调色**不超过 2 种** |
| 2 | 强调任务流程 | 流程条**仅在流程页**显示，**不常驻全站**；滚动收起为单行摘要；首页不堆功能卡 |
| 3 | AI 结果易理解 | **禁止裸 JSON / 裸段落**；AI 输出必须结构化（清单/表格/卡片），**每条 ≤2 行** |
| 4 | 关键判断显示证据 | 任何 AI 判断旁**必须挂「依据」入口**，点击展开到**原文行**并高亮 |
| 5 | 状态明确 | 五种状态各有独立视觉与文案：加载/处理中/成功/失败/空数据。**禁止用 toast 代替状态区** |
| 6 | **不用花哨 AI 视觉** | **禁用**：渐变光晕、粒子、霓虹、打字机逐字动效、"AI 思考中"科幻动画。加载用**分步文字 + 骨架屏** |
| 7 | 桌面优先兼顾移动 | 断点 ≥1280 三栏 / 1024–1279 两栏 / <1024 单栏 + 底部操作条。**移动端做任务级降级，不做等比压缩** |

---

## 5. 视觉禁则（impeccable 通用建议与项目的冲突处，以本节为准）

impeccable 的默认 DO/DON'T 面向"最大化视觉冲击"的落地页/作品集场景，
**JobPilot 是任务型生产力工具，以下通用建议在本项目不适用**：

| impeccable 通用建议 | 本项目裁定 | 依据 |
|---|---|---|
| 使用独特展示字体 + clamp() 流体大字 | ❌ **不采用**。字阶固定 12/13/14/16/20/24，字重仅 400/500 | 规范 §1.2 |
| 用 oklch / color-mix 生成色彩 | ❌ **不采用**。色彩是**冻结的令牌表**（已逐色实算 WCAG 对比度） | 规范 §1.1 |
| 追求"大胆的美学方向""让人问这是怎么做出来的" | ❌ **不采用**。目标是"让人相信数据是真的"，不是视觉惊艳 | 规范 §0 原则 6 |
| 用 box-shadow 做深度层次 | ❌ **禁用**。改用 `border` 与底色差异（打印与低端屏更稳） | 规范 §1.2 |
| 渐变、光晕、装饰性动效 | ❌ **禁用**（见原则 6） | 规范 §0 |

**本项目真正适用的 impeccable 能力域**：布局间距修复（`arrange`）、排版层次（`typeset`）、
无障碍审查（`audit`）、UX 文案（`clarify`）、最终打磨（`polish`）、生产加固（`harden`）、
规范化（`normalize`）、精简去噪（`distill`）。

**特别注意**：`bolder` / `colorize` / `overdrive` / `delight` 这四个"增强视觉"模块
与本项目原则 6 直接冲突，**不得使用**。

---

## 6. 不可动的冻结资产

- `app/globals.css` 顶部的 CSS 变量令牌表（颜色、圆角、行高、间距）——**逐值已实算验证**，不得改值。
- 四态事实芯片 `FactChip` 的**「颜色 + 形状 + 文字」三重区分**——无障碍硬要求，不得简化为纯颜色。
- 语义色仅作填充，文字一律用 `--text-*` 变体（原始填充色对比度不达标，实测 2.50–3.94:1）。
- **禁止出现任何百分比数字**（覆盖计数用 `4/6` + 色块，产品决策：不给伪精确感）。
- PDF 导出仅渲染 `CONFIRMED`，不使用语义色。

## 7. 无障碍基线（WCAG AA 为下限，不可降）

- 正文对比度 ≥ 4.5:1；大字/图形 ≥ 3:1
- 触控目标 ≥ 44×44px（移动端）；桌面行内按钮 ≥ 32px 且四周留 8px
- 全功能可 Tab 到达；`Enter` 主操作、`Esc` 关闭弹层；焦点环 `2px solid var(--color-primary)` + `outline-offset: 2px`
- 表格用真实 `<table>` + `<th scope>`；状态芯片带 `aria-label` 完整描述
- 遵循 `prefers-reduced-motion`
- 200% 缩放不破版

## 8. 微文案基调

- 错误文案见规范 §3 对照表（已定稿，含中文文案与可行动作），**不得改写**。
- 越权访问**统一走 404**，不做"无权限"提示（避免资源枚举）。
- 文案原则：**不甩锅给用户、不暴露技术细节、每个词都有存在价值**。
- 按钮用动词（「重新解析」而非「提交」）；空状态必须说明「为什么空」并给出下一步。
